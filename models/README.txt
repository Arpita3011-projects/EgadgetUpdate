This folder is auto-populated when you run:
    python src/train_model.py

Files created:
    random_forest_model.pkl  — trained Random Forest classifier
    scaler.pkl               — fitted MinMaxScaler
    feature_names.pkl        — list of feature column names
